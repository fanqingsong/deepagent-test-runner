# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth/login.spec.ts >> Login >> forgot password link switches to password reset
- Location: tests/e2e/auth/login.spec.ts:51:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('text=Reset Your Password')
Expected: visible
Error: strict mode violation: locator('text=Reset Your Password') resolved to 3 elements:
    1) <p>Reset Your Password</p> aka getByRole('paragraph').filter({ hasText: /^Reset Your Password$/ })
    2) <h2>Reset Your Password</h2> aka getByRole('heading', { name: 'Reset Your Password' })
    3) <p>Enter your email address and we'll send you a lin…</p> aka getByText('Enter your email address and')

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('text=Reset Your Password')

```

# Page snapshot

```yaml
- generic [ref=e4]:
  - generic [ref=e5]:
    - heading "AI Test Runner" [level=1] [ref=e6]
    - paragraph [ref=e7]: Reset Your Password
  - generic [ref=e9]:
    - generic [ref=e10]:
      - heading "Reset Your Password" [level=2] [ref=e11]
      - paragraph [ref=e12]: Enter your email address and we'll send you a link to reset your password.
    - generic [ref=e13]:
      - generic [ref=e14]: Email Address *
      - textbox "Email Address required" [ref=e15]:
        - /placeholder: you@example.com
    - button "Send Reset Link" [ref=e16] [cursor=pointer]
    - button "Back to Login" [ref=e17] [cursor=pointer]
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | import { loginViaApi, TEST_USER } from '../../fixtures/test-helpers';
  3  | 
  4  | test.describe('Login', () => {
  5  |   test.beforeEach(async ({ page }) => {
  6  |     await page.goto('/#login');
  7  |   });
  8  | 
  9  |   test('shows login form with email and password fields', async ({ page }) => {
  10 |     await expect(page.locator('h1')).toContainText('AI Test Runner');
  11 |     await expect(page.locator('#login-email')).toBeVisible();
  12 |     await expect(page.locator('#login-password')).toBeVisible();
  13 |     await expect(page.locator('button[type="submit"]')).toContainText('Sign In');
  14 |   });
  15 | 
  16 |   test('validates empty email field', async ({ page }) => {
  17 |     await page.locator('#login-password').fill('somepassword');
  18 |     await page.locator('button[type="submit"]').click();
  19 |     await expect(page.locator('.error-message, [role="alert"]')).toBeVisible();
  20 |   });
  21 | 
  22 |   test('validates empty password field', async ({ page }) => {
  23 |     await page.locator('#login-email').fill('test@example.com');
  24 |     await page.locator('button[type="submit"]').click();
  25 |     await expect(page.locator('.error-message, [role="alert"]')).toBeVisible();
  26 |   });
  27 | 
  28 |   test('shows error on invalid credentials', async ({ page }) => {
  29 |     await page.locator('#login-email').fill('wrong@test.com');
  30 |     await page.locator('#login-password').fill('wrongpassword');
  31 |     await page.locator('button[type="submit"]').click();
  32 |     await expect(page.locator('.error-message, [role="alert"]')).toBeVisible();
  33 |   });
  34 | 
  35 |   test('successful login redirects to dashboard', async ({ page }) => {
  36 |     await loginViaApi(page);
  37 |     await page.goto('/#dashboard');
  38 |     await page.waitForURL(/#dashboard/);
  39 |     await expect(page.locator('text=测试仪表板')).toBeVisible();
  40 |   });
  41 | 
  42 |   test('logout redirects to login page', async ({ page }) => {
  43 |     await loginViaApi(page);
  44 |     await page.goto('/#dashboard');
  45 |     await page.waitForURL(/#dashboard/);
  46 | 
  47 |     await page.locator('button:has-text("退出登录")').click();
  48 |     await expect(page).toHaveURL(/#login/);
  49 |   });
  50 | 
  51 |   test('forgot password link switches to password reset', async ({ page }) => {
  52 |     await page.locator('button:has-text("Forgot password")').click();
> 53 |     await expect(page.locator('text=Reset Your Password')).toBeVisible();
     |                                                            ^ Error: expect(locator).toBeVisible() failed
  54 |   });
  55 | });
  56 | 
```